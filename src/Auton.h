#include "Shared.h"
#include "DriveBase.h"



class Auton {
	// Initialize the drivebase, set's starttime to current time, set's motor speed to half
	static void init();

	// Loops through for the whole time of periodic
	static void periodic();
};
